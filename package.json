{
  "name": "cc-wiki",
  "version": "1.0.0",
  "description": "Confidential Computing wiki hub — TWI SIG and linux-coco mailing list archives, served via repoview.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "repoview": "^0.5.1"
  },
  "scripts": {
    "start": "repoview --repo data --host 0.0.0.0 --port ${PORT:-3000} --no-watch"
  }
}
