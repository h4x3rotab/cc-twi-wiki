# Cookie copied from a logged-in groups.io browser session.
# Required because groups.io blocks unauthenticated/non-Chrome TLS clients
# with a "Groups.io AI Crawler Policy" 402 page. The scraper uses curl_cffi
# to mimic Chrome's TLS fingerprint and sends this cookie for auth.
GROUPSIO_COOKIE=
